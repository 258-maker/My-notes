# 外部中断配置
![](../图片/IMG_20260213_111554%201.jpg)
1. 中断源有中断请求；
2. 中断源的中断允许位为1；
3. CPU开中断（即EA=1）；
```c
EA=1;//打开总终端开关
EX0=1;//开外部中断0
IT0=0/1;//设置外部中断的触发方式
void int() interrupt 0 using 1
{
	//编写用户所需的功能代码
}
```

